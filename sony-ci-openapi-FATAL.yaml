# DEMO ARTIFACT - this one genuinely fails.
#
# Unlike sony-ci-openapi-SLOPPY.yaml, which Airia accepts, this file does not
# survive the YAML parse at all. mcp-link unmarshals YAML before it looks at
# anything else, so a syntax fault is one of the few things that actually stops
# a spec becoming an MCP server.
#
# Faults: unterminated quoted string, and a mapping value that opens a flow
# sequence and never closes it.
openapi: 3.0.3
info:
  title: "Sony Ci Media Cloud (fatal
  version: 1.0.0
servers:
  - url: https://api.cimediacloud.com
paths:
  /workspaces:
    get:
      operationId: listUserWorkspaces
      summary: List workspaces
      parameters: [ {name: limit, in: query
      responses:
        '200':
          description: OK
